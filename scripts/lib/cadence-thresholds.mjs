/**
 * Пороги AI-каденции для invest-gulf (английский корпус, ОАЭ и Залив).
 *
 * Движок `scripts/lib/ai-cadence.mjs` и CLI `scripts/cadence-check.mjs` общие с
 * moregroupestate-ru и more-group-website, байт в байт. Разница между сайтами
 * живёт только здесь: язык шаблонов и потолки по коллекциям. Расхождение копий
 * движка ловит `npm run cadence:parity`.
 *
 * КАК МЕНЯТЬ ПОРОГИ. Числа посчитаны командой `node scripts/cadence-check.mjs
 * --calibrate --json` по живому корпусу и одним детерминированным правилом для
 * всех сайтов сети:
 *   maxPer1k        = max(2.5, p90 плотности, округлённый до ближайших 0.5);
 *   maxMetronomePct = p90 метронома, округлённый ВВЕРХ до кратного 5, в пределах 20..60;
 *   minCv           = (p10 CV минус 0.05), округлённый ВНИЗ до 0.05, в пределах 0.25..0.35.
 * Профиль раздела заводится только при n >= 8 файлов длиннее порога слов.
 * Порог из головы здесь запрещён: гейт по GEO-баллу, выставленный до замера,
 * кончился тем, что машинные тексты набирали 92, а рукописные 78.
 *
 * Языковых разделов не на английском на сайте нет: все шесть коллекций
 * проверены по тексту, кириллицы и арабского письма в прозе нет (в одном гайде
 * 26 кириллических символов, это вкрапление в английский текст).
 */

export const LANG = 'en';

/** Замер 22.09.2026, 616 файлов длиннее 250 слов, ветка cc/cadence-port. */
export const CALIBRATION = {
  measuredAt: '2026-09-22',
  files: 616,
  corpus: {
    per1k: { p50: 2.12, p75: 3.08, p90: 4.24, max: 9.82 },
    metronomePct: { p50: 16, p75: 25, p90: 35, max: 82 },
    cv: { p10: 0.47, p25: 0.54, p50: 0.63 },
  },
  /** Сумма попаданий по категориям на весь корпус. */
  categoryTotals: {
    antithesis: 2423,
    colonHook: 104,
    superlativeOpener: 36,
    calque: 9,
    clerical: 1,
    headingHook: 1,
    literary: 0,
    impersonal: 0,
  },
  /**
   * Вывод тот же, что на Пхукете: почти всю плотность даёт ОДНА фигура речи,
   * антитеза («X, not Y», «not X but Y») - 2423 попадания из 2574, это 94%
   * всех маркеров и 3.93 на файл. Канцелярит и пафос, которые ловят словари,
   * дают на весь корпус 1 и 0 попаданий. Невидимых символов в корпусе нет
   * ни одного, поэтому жёсткая проверка в prebuild сейчас ничего не блокирует.
   */
  dominantCategory: 'antithesis',
};

export const CADENCE_THRESHOLDS = {
  // p90 4.24 (p50 2.12, p75 3.08), до ближайших 0.5 это 4.0
  maxPer1k: 4.0,
  // p90 35.07% (p50 16, p75 25), вверх до кратного 5 это 40
  maxMetronomePct: 40,
  // p10 CV 0.47, минус 0.05 = 0.42, вниз до 0.40, потолок правила 0.35
  minCv: 0.35,
  maxParallel: 3,
  // Повтор n-граммы шумит на шаблонных карточках проектов: только отчёт.
  maxRepeated: null,
  // Ниже 300 слов плотность на 1000 слов перестаёт быть устойчивой: четыре
  // попадания на 264 слова дают 15/1k и выглядят как провал на ровном месте.
  minWords: 300,
};

/**
 * Профили по коллекциям, каждый по своим квантилям и тем же формулам.
 * Все шесть разделов набрали n >= 8, профиль есть у каждого.
 */
export const COLLECTION_PROFILES = {
  // n=444: плотность p75 3.06 / p90 4.10, метроном p75 26 / p90 37.4, CV p10 0.48
  guides: { maxPer1k: 4.0, maxMetronomePct: 40, minCv: 0.35 },
  // n=71: плотность p75 2.61 / p90 4.27, метроном p75 18 / p90 28.1, CV p10 0.48
  areas: { maxPer1k: 4.5, maxMetronomePct: 30, minCv: 0.35 },
  // n=35: плотность p75 3.97 / p90 5.76, метроном p75 21 / p90 27.8, CV p10 0.49;
  // страницы застройщиков (developers-*), хвост по плотности самый длинный
  hubs: { maxPer1k: 6.0, maxMetronomePct: 30, minCv: 0.35 },
  // n=29: плотность p75 3.12 / p90 3.87, метроном p75 25 / p90 25.6, CV p10 0.48
  compare: { maxPer1k: 4.0, maxMetronomePct: 30, minCv: 0.35 },
  // n=25 длиннее 400 слов: плотность p75 2.99 / p90 3.54, метроном p75 18 / p90 25.5,
  // CV p10 0.46; карточки проектов, поэтому 400 слов и 4 параллельных старта
  projects: { maxPer1k: 3.5, maxMetronomePct: 30, minCv: 0.35, minWords: 400, maxParallel: 4 },
  // n=12 длиннее 350 слов: плотность p75 4.19 / p90 4.42, метроном p75 15 / p90 23.1,
  // CV p10 0.40 (минус 0.05 = 0.35); короткие новости
  news: { maxPer1k: 4.5, maxMetronomePct: 25, minCv: 0.35, minWords: 350 },
};

export function cadenceProfileFor(collection) {
  return { ...CADENCE_THRESHOLDS, ...(COLLECTION_PROFILES[collection] || {}) };
}
